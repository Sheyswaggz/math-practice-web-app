# Application Environment Configuration
# Copy this file to .env and customize values for your local environment

# ============================================
# Application Settings
# ============================================

# Environment mode: development, staging, production
NODE_ENV=development

# Port the application listens on
PORT=8080

# ============================================
# Database Configuration
# ============================================

# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:password@localhost:5432/mathpractice

# ============================================
# Redis Configuration
# ============================================

# Redis connection string for caching and session storage
# Format: redis://host:port
REDIS_URL=redis://localhost:6379

# ============================================
# Authentication & Security
# ============================================

# JWT secret key for signing tokens
# IMPORTANT: Change this to a strong random string in production
# Generate with: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-in-production

# JWT access token expiration time
# Examples: 15m, 1h, 24h
JWT_EXPIRY=15m

# Refresh token expiration time
# Examples: 7d, 30d, 90d
REFRESH_TOKEN_EXPIRY=7d

# ============================================
# AWS Configuration
# ============================================

# AWS region for services
AWS_REGION=us-east-1

# S3 bucket name for storing math problem images and assets
AWS_S3_BUCKET=math-practice-images

# AWS credentials (optional if using IAM roles)
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key

# ============================================
# CORS Configuration
# ============================================

# Allowed origin for CORS requests
# For local development with React frontend on port 3000
CORS_ORIGIN=http://localhost:3000

# ============================================
# Logging Configuration
# ============================================

# Log level: error, warn, info, http, verbose, debug, silly
LOG_LEVEL=info

# ============================================
# Optional: Additional Configuration
# ============================================

# Session secret for express-session
# SESSION_SECRET=your-session-secret-change-in-production

# Maximum file upload size in bytes (e.g., 5242880 = 5MB)
# MAX_FILE_SIZE=5242880

# Rate limiting: maximum requests per window
# RATE_LIMIT_MAX=100

# Rate limiting: time window in milliseconds (e.g., 900000 = 15 minutes)
# RATE_LIMIT_WINDOW=900000

# Email service configuration (if using email notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# EMAIL_FROM=noreply@mathpractice.com